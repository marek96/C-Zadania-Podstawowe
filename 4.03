#include <iostream>
#include <cstdlib>
#include <ctime>
#include <conio.h>

using namespace std;

int main()
{
    //srand(time(NULL));
    int tab[10][10];

    for(int i=0;i<10;i++){
        for(int j=0;j<10;j++){
                if(i==j){
                    tab[i][j]=1;
                    cout.width(2);
                    cout << tab[i][j];
                }
                else{
                    tab[i][j] = 0;
                    cout.width(2);
                    cout << tab[i][j];
                }
        }
        cout << endl;
    }
